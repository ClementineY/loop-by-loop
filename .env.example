# Absolute public origin used for canonical and social metadata.
# Production account-site value; local development can leave this unset.
SITE_URL=https://clementiney.github.io

# Usually leave this as /. Set only when intentionally hosted under a subpath.
BASE_PATH=/loop-by-loop
