<script lang="ts">let mode='inference'; const items=[['model.state_dict()','learned weights and buffers'],['optimizer.state_dict()','momentum / Adam history'],['epoch','where training stopped'],['config + best metric','how the run was defined']];</script>
<section class="lab" aria-labelledby="checkpoint-title"><header><div><span>Checkpoint lab</span><h3 id="checkpoint-title">Save enough state for the job you will resume</h3></div><div><button class:active={mode==='inference'} onclick={()=>mode='inference'}>Run inference</button><button class:active={mode==='resume'} onclick={()=>mode='resume'}>Resume training</button></div></header>
  <div class="state">{#each items as item,i}<article class:needed={mode==='resume'||i===0}><span>{mode==='resume'||i===0?'✓':'—'}</span><div><code>{item[0]}</code><p>{item[1]}</p></div></article>{/each}</div>
  <div class="timeline"><article><small>before save</small><b>parameters + optimizer motion</b></article><i>save → load</i><article><small>after load</small><b>{mode==='resume'?'same location and motion':'same predictions'}</b></article></div>
  <footer>{#if mode==='inference'}<strong>Weights are sufficient for prediction.</strong><span>Reconstruct the architecture, load weights, call `eval()`, and verify a fixed input.</span>{:else}<strong>A full checkpoint preserves the trajectory.</strong><span>Without optimizer state, momentum and adaptive running statistics restart from zero.</span>{/if}</footer></section>
<style>
  .lab{margin:2rem 0;border:1px solid var(--border);border-radius:16px;background:var(--surface);overflow:hidden}header{display:flex;justify-content:space-between;align-items:center;gap:1rem;padding:1rem}header span{font:800 .6rem var(--font-mono);text-transform:uppercase;letter-spacing:.1em;color:var(--accent)}header h3{margin:.2rem 0}header button{border:1px solid var(--border);background:var(--bg);color:var(--text);padding:.4rem;font-size:.63rem}header button.active{background:var(--accent);color:white}.state{display:grid;grid-template-columns:repeat(4,1fr);gap:1px;background:#303949}.state article{display:flex;gap:.5rem;padding:.8rem;background:#121925;color:#6f7a8b}.state article.needed{color:white}.state article>span{color:var(--accent-light)}.state code{font-size:.62rem}.state p{margin:.25rem 0 0;color:#8d99aa;font-size:.6rem}.timeline{display:grid;grid-template-columns:1fr auto 1fr;align-items:center;gap:1rem;padding:1rem}.timeline article{padding:.6rem;border:1px solid var(--border);border-radius:7px}.timeline small{display:block;color:var(--text-muted);font:.53rem var(--font-mono)}.timeline b{font-size:.72rem}.timeline>i{font:normal .62rem var(--font-mono);color:var(--accent)}footer{display:flex;gap:1rem;padding:.8rem 1rem;background:var(--accent-soft);font-size:.67rem}footer span{color:var(--text-muted)}@media(max-width:650px){header{align-items:flex-start;flex-direction:column}.state{grid-template-columns:1fr 1fr}.timeline{grid-template-columns:1fr}.timeline>i{text-align:center}footer{flex-direction:column}}
</style>
